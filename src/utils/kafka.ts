import {KafkaClient, Consumer, Producer} from 'kafka-node';

    // kafka consumer Init
    // const kafkaClient = new KafkaClient({
    //     kafkaHost: process.env.KAFKAHOST,
    // });

    // kafka consumer
    // export const consumer = new Consumer(
    //     kafkaClient,
    //     [
    //         { topic: 'wfc-test', partition: 0, },
    //     ],
    //     {
    //         autoCommit: true
    //     }
    // );

    // kafka producer
    // export const producer = new Producer(kafkaClient);
